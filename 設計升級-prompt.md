# 任務：龍獅體育會網站「視覺升級」(只改排版，不改文字)

你是一位資深網頁設計師，擅長傳統文化品牌的現代化設計。
這是一個台灣舞龍舞獅文化表演團隊的網站，目標是「接案詢問優先」+「傳統莊嚴的廟會文化感」。

---

## 🔒 最高原則 (絕對不可違反)

1. **禁止修改任何文字內容** — 所有中文標題、內文、按鈕文字、聯絡資訊一律原封不動。
   如果你認為某段文字需要改動才能配合設計，**先停下來列出來問我**，得到同意才改。

2. **沿用現有的 CSS 變數** — base.css 裡已有完整色彩系統：
   `--bg`, `--paper`, `--paper-strong`, `--surface-soft`, `--ink`, `--ink-soft`, `--ink-muted`,
   `--accent` (#8f3a2f), `--accent-deep` (#763126), `--accent-soft`, `--gold` (#b9925e),
   `--line`, `--shadow`, `--radius`, `--max-width`, `--font-body`, `--font-serif`。
   直接使用這些變數，**不要新增其他顏色**。

3. **只改 CSS 排版**，HTML 結構盡量保留；非必要不動 JS。
   若必須調整 HTML class，先說明原因。

---

## 🎯 要解決的三個問題 (我目前的痛點)

1. **方框太多、框中框中框** — 卡片有外框、內框、圖片又有框，視覺被切割得很煩。
2. **全部置中對齊** — 標題、內文、按鈕全置中，有「簡報感」，不專業。
3. **層次太平、沒有呼吸節奏** — 每個區塊間距都一樣，畫面很悶。

---

## ✅ 設計規範 (請嚴格套用到每一頁)

### 排版
- **中文內文一律靠左對齊** (`text-align: left`)。只有 hero 主標可置中或靠左。
- **拆掉多餘邊框**：卡片不要 border，改用淡底色 (`var(--paper)` / `var(--surface-soft)`)
  或極淡陰影 (`var(--shadow)`) 來區隔。圖片直接貼齊卡片邊緣、不要框 (`border: 0`)。
- **一個元素最多一層框**，目標是 0 層框。

### 間距節奏
- 區塊與區塊之間用「大留白」(例如 section `padding: 4rem 1.5rem` 以上)。
- 標題與其內文「貼近」(小間距，`margin` 約 `0.5rem`)，表示它們是同一組。
- 用間距大小來分組，不要全部一樣。

### 圖片
- 活動照片放大、滿版於卡片寬度，文字可壓在圖片下緣的深色漸層上
  (提升氣勢與專業感)。

### 傳統莊嚴的點綴 (取代方框)
- 標題可加 **一條細金線** (`border-left: 3px solid var(--gold); padding-left`)
  或保留現有的卯榫角標 (corner bracket) 來點題。
- **朱紅 (`var(--accent)`) 整頁只用在 1–2 個最重要的行動按鈕** (如「立即洽詢」)，
  其餘用墨黑與金，讓紅色保有份量。
- 標題建議用襯線體 (明體 `var(--font-serif)`)，內文用黑體，增加文化質感。

---

## 📋 執行方式

1. 先從 **index.html (首頁)** 開始改。
2. 改完後，用一句話說明你「改了哪些 CSS、為什麼」，方便我理解。
3. 確認首頁 OK 後，再依同樣規範套用到 about / services / gallery / contact。
4. 任何牽涉到「改文字」「改 HTML 結構」「加新顏色」的決定，一律先問我。

請開始，從 index.html 著手。
